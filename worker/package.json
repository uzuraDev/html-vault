{
  "name": "html-vault-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HTML Vault — self-hosted HTML snippet vault on Cloudflare Workers + KV",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "setpass": "node setpass.mjs",
    "setpass:local": "node setpass.mjs --local"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
