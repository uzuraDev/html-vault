# ビルドコンテキストから除外（イメージを小さく・秘密を入れない）
node_modules
data
.env
.git
.gitignore
npm-debug.log*
Dockerfile
.dockerignore
docker-compose.yml

# OSS配布物のうちイメージに不要なもの
.github
deploy
*.md
fly.toml
railway.json
render.yaml

# CI 専用のもの。Dockerfile は scripts/ ごと COPY するので、
# ここで除外しないと検査スクリプトまで本番イメージに焼き込まれる
# （scripts/ で実行時に要るのは build-i18n.mjs だけ）。
test
scripts/check-inline-js.mjs
